public class GarbageCollectorDemo {
    public static void main(String[] args) {
        MemoryAllocator allocator = new MemoryAllocator();
        allocator.allocateMemory(); // Start allocating memory
    }
}
